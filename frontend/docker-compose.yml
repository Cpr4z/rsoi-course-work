version: '3.1'

services:
  dips-client:
    image: ghcr.io/cpr4z/frontend:latest
    container_name: dips-client
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3000:80"